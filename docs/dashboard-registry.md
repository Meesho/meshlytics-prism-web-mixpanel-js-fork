# Dashboard Registry

`mixpanel-browser` is a shared library with no deployed runtime. There are no Grafana
dashboards or Prometheus metrics associated with this package.

For runtime monitoring, refer to the Grafana dashboards of consuming services.
